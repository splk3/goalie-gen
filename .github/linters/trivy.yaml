---
exit-code: 1
exit-on-eol: 2
scan:
  disable-telemetry: true
  scanners:
    - vuln
    - misconfig
    - secret
# Only fail on CRITICAL vulnerabilities; HIGH/MEDIUM are in Gatsby dev
# dependencies with no available fixes (transitive lockfile dependencies).
severity:
  - CRITICAL
ignore-unfixed: true
